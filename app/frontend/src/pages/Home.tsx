import React from "react";
import { Card } from "react-bootstrap";
import { NavBar } from "./components/NavBar";

export const Home = () => {
  return (
    <Card>
        Home Card
        <NavBar/>
    </Card>
  );
};
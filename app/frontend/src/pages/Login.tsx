import React from "react";
import { Card } from "react-bootstrap";
import { NavBar } from "./components/NavBar";

export const Login = () => {
  return (
    <Card>
        Login Card
        <NavBar/>
    </Card>
  );
};